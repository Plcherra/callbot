/// Layout constants for responsive design.
/// Use for max-width containers on tablet, web, and desktop.
class LayoutConstants {
  LayoutConstants._();

  /// Maximum content width for centered layouts on wide screens.
  /// Content will be centered with this max width on tablet/desktop.
  static const double maxContentWidth = 800.0;
}
